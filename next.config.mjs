/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/next.js-login-page',
  assetPrefix: '/next.js-login-page',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
